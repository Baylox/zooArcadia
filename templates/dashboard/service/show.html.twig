{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ service.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_service_index') }}">Retour à la liste des services</a>

    <a href="{{ path('dashboard_service_edit', {'id': service.id}) }}">Modifier</a>

    {{ include('dashboard/service/_delete_form.html.twig') }}
{% endblock %}
