{% extends 'base.html.twig' %}

{% block title %}Habitat{% endblock %}

{% block body %}
    <h1>Habitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ habitat.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ habitat.description }}</td>
            </tr>
            <tr>
                <th>Type d'Habitat</th>
                <td>{{ habitat.typeHabitat }}</td>
            </tr>
        </tbody>
    </table>

    <div class="actions">
        <a href="{{ path('dashboard_habitat_index') }}" class="btn btn-primary">Retour à la liste</a>
        <a href="{{ path('dashboard_habitat_edit', {'id': habitat.id}) }}" class="btn btn-secondary">Modifier</a>
    </div>

    {{ include('dashboard/habitat/_delete_form.html.twig') }}
{% endblock %}
