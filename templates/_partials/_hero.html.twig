<div class="hero-scene text-center text-white">
    <div class="hero-scene-content d-flex flex-column justify-content-center align-items-center">
        <p class="fs-4">{{ intro_text | default('Partez à la découverte du monde animal au') }}</p>
        <h1 class="display-3 fw-bold">{{ title | default('ZOO ARCADIA') }}</h1>
    </div>
</div>


